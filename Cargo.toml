[package]
name = "memcache"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"], optional = true }
bincode = "1.3.3"
serde = { version = "1.0", features = ["derive"] }
